# SampleWebviewPackage

## Instalation

To install this package, import `url here` in SPM.

## Usage example

```swift

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Text centered")
                .centreHorizontally()
        }
    }
}

```

