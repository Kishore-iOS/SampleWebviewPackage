import XCTest
@testable import SampleWebviewPackage

final class SampleWebviewPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SampleWebviewPackage().text, "Hello, World!")
    }
}
